server:
    port: 8080
    servlet:
        context-path: /api

spring:
    application:
        name: lib-manage-admin
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/lib-manage-admin?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
        username: root
        password: 123456
        flyway:
            enabled: true
            locations: classpath:db/migration

mybatis-plus:
    configuration:
        map-underscore-to-camel-case: true
    global-config:
        db-config:
            logic-delete-field: deleted
    mapper-locations: classpath:mapper/**.xml

jwt:
    secret: c2VjcmV0LWtleS1mb3ItbGliLW1hbmFnZS1hZG1pbi1qd3Qtc2lnbmluZw==